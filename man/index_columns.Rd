% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{index_columns}
\alias{index_columns}
\title{Order columns by dictionary variables}
\usage{
index_columns(data, dictionary = NULL, keep_unmatched = TRUE)
}
\arguments{
\item{data}{A data frame or tibble whose columns will be reordered.}

\item{dictionary}{A \link[=data_dictionary]{data dictionary} object. If \code{NULL}, uses the default dictionary set via \code{\link[=set_default_dictionary]{set_default_dictionary()}}.}

\item{keep_unmatched}{Logical. If \code{TRUE} (the default), columns in
\code{data} that are not defined in the dictionary are retained and
appended after the matched columns. If \code{FALSE}, those columns are
dropped from the output.}
}
\value{
The same type as \code{data}, with columns reordered (and
optionally filtered) according to the dictionary.
}
\description{
Reorders the columns of a data frame so that columns whose names appear
in a \code{DataDictionary} come first, in the order they are defined in the
dictionary. Columns not present in the dictionary are optionally kept
at the end.
}
\examples{

dd <- data_dictionary(
  numeric_variable("bill_length_mm", label = "Bill length", units = "mm"),
  numeric_variable("bill_depth_mm",  label = "Bill depth",  units = "mm"),
  nominal_variable("species", label = "Species",
                   category_levels = c("Adelie", "Chinstrap", "Gentoo"))
)

df <- data.frame(
  species        = c("Adelie", "Chinstrap"),
  extra_col      = 1:2,
  bill_depth_mm  = c(18.7, 17.4),
  bill_length_mm = c(39.1, 46.5)
)

# Matched columns appear in dictionary order; extra_col is appended
index_columns(df, dictionary = dd)

# Drop columns not in the dictionary
index_columns(df, dictionary = dd, keep_unmatched = FALSE)

}
