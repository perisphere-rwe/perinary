% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_term_key}
\alias{get_term_key}
\title{Get Term Key for Nominal Variables}
\usage{
get_term_key(
  dictionary,
  adjust_to = NULL,
  term_separator = "",
  term_colname = "term"
)
}
\arguments{
\item{dictionary}{A \link[=data_dictionary]{data dictionary} object}

\item{adjust_to}{Optional. A tibble that includes a column with term names
(default: \code{'term'}). When supplied, the output is filtered to retain only
terms found in \code{adjust_to} and that match the reference level.}

\item{term_separator}{A string used to separate the variable name and
category value in the output term column. Default is \code{""}.}

\item{term_colname}{A character string giving the column name to use
for the terms column in the returned tibble. Default is \code{"term"}.}
}
\value{
A tibble with one row per variable-category combination and
columns for the variable, category type (\code{levels} or \code{labels}),
and term. If \code{adjust_to} is provided, the tibble is filtered
accordingly and includes the \code{category} column.
}
\description{
Returns a tibble linking each level or label of a nominal variable
to a modeling term, using a specified separator between the variable
name and category value. This is especially useful for joining
dictionary information to model output, such as coefficients or terms
in regression tables.
}
\examples{
dd <- as_data_dictionary(iris)
get_term_key(dd)

}
