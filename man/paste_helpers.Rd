% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paste.R
\name{paste_helpers}
\alias{paste_helpers}
\alias{paste_collapse}
\alias{paste_named_vec}
\title{Internal paste helpers}
\usage{
paste_collapse(x, as_code = FALSE)

paste_named_vec(x, name_fill = "MISSING_NAME")
}
\arguments{
\item{x}{A vector (character, numeric, etc.) to collapse. \code{NULL}
returns \code{"none"}. Length-1 values are coerced to character and
returned unchanged.}

\item{name_fill}{Character scalar used to replace missing or empty
names when formatting (default \code{"MISSING_NAME"}).}
}
\value{
\code{paste_collapse()} returns a single character scalar with elements
of \code{x} collapsed by \code{", "} and a natural \code{"and"} before the last item.

\code{paste_named_vec()} returns:
\itemize{
\item If \code{x} is a \strong{list}: a named character vector, where each element is
of the form \code{"name = c(v1, v2, ...)"}. Each element name corresponds
to the original list element name (or \code{name_fill} if missing/empty).
\item If \code{x} is an \strong{atomic vector}: a single character scalar of the form
\code{"name1 = value1, name2 = value2, ..."}.
}
}
\description{
Small utilities to format vectors/lists for readable messages and
logs. Used throughout \code{perinary} to make assertion and warning text
concise and human-friendly.
\itemize{
\item \strong{\code{paste_collapse()}}: collapse a vector with commas and a natural
language conjunction before the last element (e.g., \code{"a, b, and c"}).
\item \strong{\code{paste_named_vec()}}: format a (named) vector or list as
\code{"name = value"} pairs (lists are rendered as \code{name = c(v1, v2, ...)}),
filling in missing/empty names with a sentinel.
}
}
\section{Conventions}{

\itemize{
\item \code{NULL} inputs are rendered as \code{"none"} to avoid noisy \code{NULL} prints.
\item The last separator is \code{" and "} for length 2, otherwise \code{", and "}.
\item Empty or missing names are replaced with \code{name_fill} (default
\code{"MISSING_NAME"}).
}
}

\keyword{internal}
