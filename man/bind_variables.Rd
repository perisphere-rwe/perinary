% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind_variables.R
\name{bind_variables}
\alias{bind_variables}
\title{Bind Variables to a Dictionary}
\usage{
bind_variables(dictionary, ..., .list = NULL)
}
\arguments{
\item{dictionary}{A \link[=data_dictionary]{data dictionary} object}

\item{...}{Named arguments. Each name becomes the variable name, and each
value is a call to a variable constructor such as \code{\link[=nominal_variable]{nominal_variable()}},
\code{\link[=numeric_variable]{numeric_variable()}}, \code{\link[=logical_variable]{logical_variable()}}, \code{\link[=identifier_variable]{identifier_variable()}}, or
\code{\link[=date_variable]{date_variable()}}. The \code{name} argument of the constructor may be omitted;
\code{bind_variables} will inject it automatically.}

\item{.list}{This argument allows \code{bind_variables} to be used programmatically and is optional. It is intended to be used as an alternative to \code{...}.. A named list of
\code{DataVariable} objects. Each element's list-entry name will be used to
set (or override) the variable name.}
}
\value{
A \link[=data_dictionary]{data dictionary} object
}
\description{
A convenience wrapper for adding one or more new variables to an existing
dictionary. Each variable's name is taken from its corresponding named
argument in \code{...}, so there is no need to pass \code{name} inside the variable
constructor itself.
}
\details{
\code{bind_variables} does \strong{not} allow overwriting variables already present
in \code{dictionary}. To modify existing variables use the \verb{set_*} family
(e.g. \code{\link[=set_labels]{set_labels()}}, \code{\link[=set_units]{set_units()}}).
}
\examples{

meta <- as_data_dictionary(iris)

meta <- meta |>
  bind_variables(
    train_test = nominal_variable(
      label = "Train/test split",
      category_levels = c("train", "test"),
      category_labels = c("Training data", "Testing data")
    )
  )

meta

}
